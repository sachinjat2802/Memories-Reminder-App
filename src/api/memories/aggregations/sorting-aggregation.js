const Memory = require("../memory-model");

const sortByEventDate = async (eventDate) => {
    try {
        
    } catch(e) {
        console.log(e);
        throw e;
    }
}

module.exports = {
    sortByEventDate,
};
